{
  "scripts": {
    "start": "node server.js",
    "dev": "live-server --port=8080 --host=localhost --watch=index.html,style.css,app.js"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  }
}
